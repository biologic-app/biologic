---
feature: [название фичи/контекста]
mode: feature
stage: plan
status: draft
updated: "ГГГГ-ММ-ДД"
---

# План: [Название фичи/задачи]

## Проблема/цель

[Что нужно сделать и зачем. Какую проблему решает, для кого.
Если это новый способ достижения существующего результата (напр. "импорт из Excel вместо ручного заполнения в UI") — явно укажи: юзкейс/результат новый или существующий, что именно меняется в точке входа.]

## Затрагивает

- **Новое / существующее:** [что уже есть в системе и переиспользуется, а что добавляется впервые]
- **Агрегаты/сущности:** [ключевые модели, инварианты — если не меняются, так и написать: "без изменений"]
- **Права доступа:** [какие роли получают доступ к новому сценарию; отличается ли от прав на существующий функционал]
- **Связанные решения:** [ADR / прошлые планы, если есть]

## Технические решения — Backend

> Для каждого решения: что делаем, почему так, ссылка на существующий код-образец (если паттерн уже используется), пример кода.

### Use Case / Application-слой

- **Паттерн:** [например, Use Case / Command Handler — как уже реализовано в проекте]
- **Ссылка на существующий пример:** `path/to/existing/UseCase.ts:LN`
- **Новый Use Case:** `[Название]UseCase`

```ts
// пример: путь предполагаемого нового файла
// src/modules/[feature]/application/[Name]UseCase.ts

class ImportProductsFromExcelUseCase {
  constructor(
    private readonly parser: ExcelParser, // новая зависимость
    private readonly repository: ProductRepository, // существующая, DI как везде
  ) {}

  async execute(command: ImportProductsCommand): Promise<ImportResult> {
    // ...
  }
}
```

### DI / регистрация зависимостей

- **Как регистрируется:** [ссылка на существующий DI-контейнер/модуль, где регистрируются use case'ы]
- **Что добавляется:** [новый провайдер/биндинг]

```ts
// src/modules/[feature]/di/module.ts
container.register(ImportProductsFromExcelUseCase, { ... });
```

### Разграничение прав

- **Механизм:** [ссылка на существующий guard/middleware/policy]
- **Что проверяется:** [роль/право, специфичное для нового сценария, если отличается от ручного ввода]

```ts
// пример guard'а или policy-проверки
@RequirePermission('catalog.import')
```

### Что происходит в системе (flow)

1. [Шаг 1 — например: файл загружается через endpoint `POST /catalog/import`]
2. [Шаг 2 — парсинг, валидация построчно]
3. [Шаг 3 — использование существующего доменного слоя (Product.create() и т.д.) — **без дублирования бизнес-логики**]
4. [Шаг 4 — что происходит при ошибке в отдельной строке: пропуск / откат всей операции / отчёт об ошибках]
5. [Шаг 5 — какие события эмитятся (переиспользуются существующие или новые)]

### Другие паттерны/инфраструктура (переиспользуемые)

- [Repository / Unit of Work — ссылка на существующую реализацию]
- [Event Bus / Domain Events — переиспользуется без изменений]
- [Валидация — существующие Value Objects/инварианты применяются как есть]

## Технические решения — Frontend

### Компонент / Composable

- **Паттерн:** [например, composable, содержащий всю логику экрана/фичи]
- **Ссылка на существующий пример похожего composable:** `src/composables/use[Similar].ts`
- **Новый composable:** `use[Name]`

```ts
// src/composables/useProductImport.ts
export function useProductImport() {
  const file = ref<File | null>(null);
  const status = ref<"idle" | "uploading" | "done" | "error">("idle");
  const errors = ref<ImportRowError[]>([]);

  async function upload() {
    // вызов API, обработка ответа, обновление status/errors
  }

  return { file, status, errors, upload };
}
```

### UI-компонент(ы)

- **Что переиспользуется:** [существующие компоненты — кнопки, таблицы ошибок, лоадеры]
- **Что новое:** [например, компонент загрузки файла]

### Взаимодействие с существующим UI

- [Как новый сценарий встраивается в существующий экран — например, кнопка "Импорт" рядом с "Добавить вручную"]

## Критерии готовности

- [ ] [измеримый критерий 1]
- [ ] [измеримый критерий 2]
- [ ] Права доступа проверены (кто может использовать новый сценарий)
- [ ] Существующие бизнес-инварианты не дублируются, а переиспользуются
- [ ] Обработка ошибок определена (частичный успех / откат)
- [ ] Тесты написаны (unit — use case, composable; интеграционные — при необходимости)
- [ ] Код прошёл review, задеплоен и проверен в staging

## Визуальные артефакты

> Приложить при реализации — для наглядности решения на ревью.

- [ ] Скриншот / GIF: новый UI-сценарий (например, форма загрузки файла)
- [ ] Видео: полный флоу от действия пользователя до результата
- [ ] Диаграмма (если меняется поток данных/архитектура)
- [ ] Пример запроса/ответа API (если появляется новый эндпоинт)

## Открытые вопросы

- ⚠ [вопрос, требующий подтверждения продукта/архитектора]
- ⚠ [вопрос 2]

## Ожидаемый результат

[Что будет на выходе с точки зрения пользователя и системы. Явно отметь: какая часть — новая инфраструктура, а какая — переиспользование существующей.]

## Related

- User Story / задача: [ссылка]
- Похожий существующий код (для паттернов): [ссылка]
- ADR: [ссылка]
- API Spec: [ссылка]
