VITE_API_BASE_URL=http://localhost:8080
VITE_API_PREFIX=/api/v1
VITE_API_REQUEST_CASE=snake

# Passive UI telemetry (ROADMAP A2.3). Off by default; anonymous, only
# element/session identifiers are collected — see useTelemetry.ts.
VITE_TELEMETRY_ENABLED=false
