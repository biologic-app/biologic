# Local development defaults. Copy to `.env` and adjust secrets before shared use.
APP_APP_NAME="Biologic System Backend API"
APP_API_V1_PREFIX="/api/v1"
APP_DEBUG=false
APP_IS_DEV=true
APP_DOCS_URL="/docs"
APP_OPENAPI_URL="/openapi.json"

# PostgreSQL 15 from docker-compose.yml.
APP_DATABASE_URL="postgresql+asyncpg://biologic:biologic@127.0.0.1:5433/biologic"

APP_JWT_SECRET_KEY="change-me-local-jwt-secret"
APP_JWT_ALGORITHM="HS256"
APP_ACCESS_TOKEN_TTL_SECONDS=40
APP_REFRESH_TOKEN_TTL_SECONDS=60
APP_ACCESS_COOKIE_NAME="access_cookie"
APP_REFRESH_COOKIE_NAME="refresh_cookie"
APP_AUTH_COOKIE_SAMESITE="lax"
APP_AUTH_COOKIE_SECURE=false
APP_AUTH_COOKIE_DOMAIN="localhost"
APP_AUTH_COOKIE_PATH="/"

# Web Push (VAPID). Generate a pair with `uv run vapid --gen` and paste the
# resulting keys below; APP_VAPID_SUBJECT must be a mailto: or https: URI.
APP_VAPID_PUBLIC_KEY=""
APP_VAPID_PRIVATE_KEY=""
APP_VAPID_SUBJECT="mailto:admin@example.com"
